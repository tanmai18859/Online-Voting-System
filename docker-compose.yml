version: "3.8"

services:
  backend:
    build: ./backend
    container_name: voting-backend
    ports:
      - "5001:5000"
    environment:
      - NODE_ENV=development

  frontend:
    build: ./frontend
    container_name: voting-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend